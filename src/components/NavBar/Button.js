import styled from "styled-components"
import { Link } from "gatsby"

const Button = styled(Link)`
    background-color: rgba(85, 188, 144, 0.75);
    padding: 1rem 2rem;
    margin-left: 1rem;
    color: white;
    border-radius: 100px;
    text-decoration: none;
`

export default Button